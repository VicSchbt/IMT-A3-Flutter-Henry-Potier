name: imt_a3_flutter_henry_potier
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.1.3
  http: ^1.0.0
  dio: ^5.2.0
  retrofit: ^4.0.1
  json_annotation: ^4.8.1
  build_runner: ^2.4.1
  build_test: ^2.1.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  retrofit_generator: ^7.0.1
  json_serializable: ^6.7.0
  build_runner: ^2.4.4
  equatable: ^2.0.5

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true
